[metadata]
version = "1.0.0"
name = "agwiki"
description = "Operates the agwiki CLI: init, new, ingest (aikit-sdk, idempotent), check, materialize; Agent Skills layout and CI expectations."

[dependencies]
